<Window x:Class="FtSdM14_CP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FtSdM14_CP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <!-- Background Grid -->
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2c3e50" Offset="0.0"/>
                <GradientStop Color="#3498db" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Button Panel -->
        <Grid Margin="20">
            <Grid.Resources>
                <!-- Button Style -->
                <Style TargetType="Button">
                    <Setter Property="Width" Value="120"/>
                    <Setter Property="Height" Value="50"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Background" Value="#1abc9c"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" BlurRadius="8"/>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#16a085"/>
                            <Setter Property="Cursor" Value="Hand"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Buttons -->
            <Button Content="Button 1" Grid.Row="0" Grid.Column="0"/>
            <Button Content="Button 2" Grid.Row="0" Grid.Column="1"/>
            <Button Content="Button 3" Grid.Row="0" Grid.Column="2"/>
            <Button Content="Button 4" Grid.Row="0" Grid.Column="3"/>

            <Button Content="Button 5" Grid.Row="1" Grid.Column="0"/>
            <Button Content="Button 6" Grid.Row="1" Grid.Column="1"/>
            <Button Content="Button 7" Grid.Row="1" Grid.Column="2"/>
            <Button Content="Button 8" Grid.Row="1" Grid.Column="3"/>

            <Button Content="Special Button" Grid.Row="2" Grid.Column="1"/>
            <Button Content="Extra Button" Grid.Row="2" Grid.Column="2"/>
        </Grid>
    </Grid>
</Window>
